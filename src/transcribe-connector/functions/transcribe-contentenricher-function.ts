// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for TranscribeContentenricherFunction
 */
export interface TranscribeContentenricherFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/transcribe-connector/functions/transcribe-contentenricher.
 */
export class TranscribeContentenricherFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: TranscribeContentenricherFunctionProps) {
    super(scope, id, {
      description: 'src/transcribe-connector/functions/transcribe-contentenricher.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/transcribe-connector/functions/transcribe-contentenricher.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}